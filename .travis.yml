sudo: required

language: generic

services:
  - docker

before_install:
  - docker build -t adilsaeed31/adilworld  .

script:
  - echo "skipping tests"

after_success:
  - echo "$DOCKER_PASSWORD" | docker login -u adilsaeed31 --password-stdin
  - docker push adilsaeed31/adilworld
